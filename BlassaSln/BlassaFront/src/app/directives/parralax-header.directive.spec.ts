import { ParralaxHeaderDirective } from './parralax-header.directive';

describe('ParralaxHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ParralaxHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
